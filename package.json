{
  "name": "pulse",
  "version": "1.0.0",
  "private": true,
  "description": "Pulse — Instagram Content Analytics (single-service deploy: Node serves the React build)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix pulse-app install --include=dev && npm --prefix pulse-app run build && npm --prefix server install --omit=dev",
    "start": "npm --prefix server start"
  }
}
